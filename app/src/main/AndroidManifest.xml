<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.admin.apptitude">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ViewQuestions"></activity>
        <activity android:name=".QuestionList" />
        <activity android:name=".DeleteCategory" />
        <activity android:name=".SelectAddcategory" />
        <activity android:name=".AddQuestions" />
        <activity android:name=".SelectAddQuestions" />
        <activity android:name=".Report" />
        <activity android:name=".AddCandidate" />
        <activity android:name=".CandidateList" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".SplashscreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>